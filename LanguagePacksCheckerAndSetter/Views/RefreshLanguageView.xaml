<UserControl x:Class="LanguagePacksCheckerAndSetter.Views.RefreshLanguageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LanguagePacksCheckerAndSetter.Views"
             xmlns:vm="clr-namespace:LanguagePacksCheckerAndSetter.ViewModels"
             xmlns:models="clr-namespace:LanguagePacksCheckerAndSetter.Models"
             xmlns:utils="clr-namespace:LanguagePacksCheckerAndSetter.Utilities"
             mc:Ignorable="d">

    <UserControl.Resources>
        <utils:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:RefreshLanguageViewModel/>
    </UserControl.DataContext>
    
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Refresh Button -->
        <Button Content="Refresh Language Settings"
                Command="{Binding RefreshLanguageCommand}"
                IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                Width="200"
                Height="40"
                HorizontalAlignment="Left"
                Margin="0,0,0,10" />

        <!-- Log Output -->
        <ListBox Grid.Row="1" ItemsSource="{Binding OperationLog}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:LogEntry}">
                    <TextBlock Text="{Binding Message}" Foreground="{Binding Foreground}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
