<UserControl x:Class="LanguagePacksCheckerAndSetter.Views.EnableDotNetFeaturesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LanguagePacksCheckerAndSetter.Views"
             xmlns:vm="clr-namespace:LanguagePacksCheckerAndSetter.ViewModels"
             xmlns:models="clr-namespace:LanguagePacksCheckerAndSetter.Models"
             mc:Ignorable="d" 
             >

    <UserControl.DataContext>
        <vm:EnableDotNetFeaturesViewModel/>
    </UserControl.DataContext>


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Help Text -->
        <TextBlock Grid.Row="0" Text="Select the CAB file from Windows installation media (usually in X:\sources\sxs)." 
                   Foreground="Gray" Margin="0,0,0,10"/>

        <!-- Browse and Enable Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Browse CAB File" Command="{Binding BrowseSourceCommand}" Width="150"/>
            <Button Content="Enable .NET 3.5 Features" Command="{Binding EnableFeaturesCommand}" Width="200" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Log Output -->
        <ListBox Grid.Row="2" ItemsSource="{Binding OperationLog}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:LogEntry}">
                    <TextBlock Text="{Binding Message}" Foreground="{Binding Foreground}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>

</UserControl>
